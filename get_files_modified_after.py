# Get the directory/file we have to check for
    # expand '.' to pwd
    # if file check in pwd, if not present throw error
# Parse the date given
    # if can't parse, throw error

# Using `date -r <filename/dirname>` to get the last modified date
# either of the file or all the files in the directory.
# If the directory has directories go into those directories and check.

# Compare the last modified date with the date given in the above
# loop.

# Print the file names to a file.
# If there is a directory in which all files are created/modified after
# the given date, print only the directory name.
